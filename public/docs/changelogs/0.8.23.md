- Fitness workouts (NIP-101e): browse and post training sessions on the new /workouts feed with stat-rich cards (distance, pace, heart rate, per-set breakdowns with real exercise names), a quick workout composer, metric/imperial unit settings that sync across devices, and Android Health Connect import that pre-fills and merges recorded sessions (read-only permission)
- Payment targets (NIP-A3): declare alternative payment addresses on your profile — Bitcoin (on-chain or silent payments), Lightning, Monero, Ethereum, Cash App, PayPal and more — rendered as tappable profile chips and zap-sheet methods with QR, copy, and wallet handoff
- Emoji picker overhaul: hundreds of emoji added from Unicode 14–17 (shaking face, low battery, wing, face with bags under eyes…), the grid drops ~2,000 skin-tone duplicates, long-press or right-click any person emoji to pick its skin tone, search also matches :shortcodes:, and category tabs no longer shift under the cursor or slide off-screen when switching
- Nests: scheduled rooms show as Planned instead of aging out, rooms hosted on custom relays no longer break presence and chat, other people's reactions render, and a final goodbye is published when you leave so you stop appearing as a speaker
- Download podcasts and music for offline playback with pause/resume, Wi-Fi-only gating on Android, per-show auto-download limits, a Downloads library filter, and positions that resume across devices and Android Auto
- Music pages load much faster with database-first tracks, background refreshes, and smarter caching for rankings and zap totals
- Fixed cursor jumping in composers while typing — most visible when quoting a note — and Android autocorrect no longer garbles or misplaces text; emoji, GIF, media and poll inserts keep the cursor in the right spot
- Overhauled @-mentions: one shared autocomplete engine across composer, search, dialogs and pickers that matches NIP-05 addresses and npubs, streams relay results live, and publishes pretty @Name links carrying each user's real relay hints so mentions actually reach them
- Search overhaul: kind:/from:/since:/until:/#tag/-exclude operators become real filters, npub/note/address pastes open the right viewer, results stream in progressively with instant local matches, and unreachable search relays show a clear banner instead of silence
- Feeds load dense on first visit: the app waits for your NIP-65 relays with a smart quorum instead of racing ahead, the Global feed gets live posts from anyone again, and slow empty relays can no longer cut a fetch short
- First-login empty-feed fixes across home, videos/live, notifications, polls, events and calendar on cold sign-in
- NIP-51 private relay lists load correctly with encrypted-content merging and NIP-04 fallback, custom indexer relays are honored, and indexer/proxy/broadcast/trusted sets persist per account
- Profile metadata now recovers from sparse or stale kind 0 snapshots instead of suppressing the author's real profile forever
- Podcast notes with Podcast-Index-unknown feed GUIDs recover their internal player card and playable episode instead of an external link
- The /events page no longer spins in a console-flooding loop on empty results, and Pinboards no longer crash during grid updates
- Bug fixes and performance improvements
