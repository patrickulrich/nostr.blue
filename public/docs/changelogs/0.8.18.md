- Retry button on "Event not found" page
- Always embed author in nevent links (even without relay hints)
- Subscribe AddressViewer to client initialization
- Bumped event-fetch timeouts for slow relays
- **Mostro P2P exchange integration (initial)**: kind 38383 NIP-69 order book now supports Mostro-sourced orders with a "Take with Mostro" button, a "Mostro only" filter toggle, and a "Create Order" entry point. A new `/settings/p2p` page manages the Mostro mnemonic, privacy mode, import/export, and reset. The `/p2p` route is gated on a NIP-78 terms-of-service agreement published to the user's own relays (with signature verification, version enforcement, and an offline-resilient local cache — an improvement over the cashu terms pattern).
- **Mostro P2P trade execution (wire layer)**: added a `MostroNodeConfig` store (NIP-78) for daemon pubkey + relays, a `Trade` record store (NIP-78) for local trade history, a complete set of `mostro_core::Message` builders (`take_sell`, `take_buy`, `add_invoice`, `fiat_sent`, `release`, `cancel`, `accept_cancel`, `rate_user`, `new_order`, `restore_session`, `last_trade_index`, `send_dm`) wired to the existing GiftWrap transport, and a `take_order` orchestrator that takes a NIP-69 order end-to-end (resolve node → allocate trade key → build message → persist `Trade` → wrap + publish GiftWrap). The "Take with Mostro" button now dispatches the take action and navigates to a stub `/p2p/trade/:order_id` detail screen. The full live trade view (chat, hold invoice panel, status timeline) lands in the next slice.
- Bug fixes and performance improvements
