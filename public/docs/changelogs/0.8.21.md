- DeFlock ALPR camera map with worldwide OpenStreetMap data, direction cones, offline area cache, and route directions
- Import existing PPQ Credit IDs into AI chat with automatic API key setup
- Mostro P2P trading upgraded to the latest chat protocol, with trader reputation and more reliable trade restore
- Faster login: relay lists restored from disk at startup, eliminating feed loading races
- Threads open at the top and restore your exact position on Back
- Pinboard NIP compliance: derived pin IDs, media pin types, and Unicode titles
- Fresher profiles with newest-wins metadata caching and outbox fallback
- Embedded comment and repost cards are now clickable
- PWA updates load the latest build instead of stale cached assets
- Sidebar, settings, and reaction preferences now apply within seconds of login instead of being delayed or missed on slow networks
- Audio plays on the first tap on Android instead of requiring a retry
- Mostro protocol messages are labeled "Mostro Sync" in the publish queue instead of showing up as failed direct messages
- Fixed a crash that could force-close the app when a relay reconnected
- Security: map place names can no longer break Directions buttons or inject scripts
- Custom search and favorite relay lists survive offline starts instead of being reset to defaults
- Removing a pin from a board now works when re-pinning from the pin form
- Profiles refresh in the background at most once a day per person instead of on every visit
- Author names and avatars appear instantly on photos and videos with far fewer network requests
- Camera map cache loads faster and stays compact during long browsing sessions
- Bug fixes and performance improvements
