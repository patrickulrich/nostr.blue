- Music Player Reactivity Overhaul — Switch to field-level Store selectors to prevent Android UI freeze; components now subscribe only to the fields they read, eliminating cascade re-renders from time updates
- Desktop Seek Bar Fix — Correct seek bar calculation (missing `rect.left` offset and `rect.width` division) causing wildly incorrect seek positions on desktop player
- Notification Performance — Parallel prefetch for notification posts using concurrent fetches instead of sequential
- Dioxus 0.7.7 Upgrade
- Blossom Cross-Platform — Port media upload, delete, and mirror from `gloo_net` to shared `reqwest` HTTP client with 15s timeout for native platforms
- NIP-10 Thread Parsing — Fix marker parsing to use `as_standardized()` with proper `Reply`/`Root`/positional tag distinction for correct thread tree construction
- Housekeeping — Remove stale backup files, add `.opencode/` to `.gitignore`
- Improved performance and stability
- Bug fixes
